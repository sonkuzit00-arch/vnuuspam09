# Обращения ДГД

Внутренний сервис учёта, маршрутизации и анализа обращений граждан. Заменяет
персональные Excel-файлы одним общим сервисом: карточка на обращение, личный
кабинет сотрудника (всё, что назначено ему — с любого канала) и живой общий
свод по всей команде.

## Стек

Next.js 14 (App Router) + TypeScript, Prisma + SQLite, NextAuth (Credentials),
Tailwind CSS + shadcn-style компоненты, framer-motion.

## Запуск

```bash
npm install
cp .env.example .env      # при необходимости поменяйте NEXTAUTH_SECRET
npx prisma migrate deploy
npm run db:seed           # демо-пользователи и примеры обращений
npm run dev
```

Откройте http://localhost:3000/login — демо-доступ (пароль `demo1234`):

- `victoria@dgd.local` — КАУ, Бабаханова Виктория (маршрутизирует все новые обращения)
- `elena.k@dgd.local` — GR, Ковешникова Елена (прорабатывает стратегию, делегирует поручения)
- `sofia@dgd.local` — ГД + администратор, Кузнецова Софья Викторовна
- `irina@dgd.local` — администратор, Астафьева Ирина
- `milana@dgd.local` — ГД, Сибирко Милана
- `yana@dgd.local` — ОП, Христенко Яна Юрьевна
- `tatiana@dgd.local` — BTL/БФ, Путиева Татьяна Викторовна (оба направления)
- `sergey@dgd.local` / `elena.s@dgd.local` — наблюдатели (Чижов Сергей, Свинарева Елена) —
  только просмотр общего свода и отчётов, без прав редактировать

## Роли и маршрут обращения

1. Обращение регистрируется (любым сотрудником) и по умолчанию уходит в
   кабинет КАУ (Бабаханова Виктория).
2. КАУ назначает формат ответа/маршрут на карточке обращения (ДЗ, типовой
   ответ, типовой с разъяснением, перенаправление депутату/комитету и т.д.) и
   либо ведёт обращение сама, либо передаёт его целиком другому ответственному
   («Передать обращение» на карточке).
3. Ответственный (например, GR Ковешникова) может, не передавая обращение
   целиком, поставить конкретное поручение другому человеку («Поручения» на
   карточке) — например, ГД Кузнецовой: подготовить и направить запрос,
   зарегистрировать в САДД, присвоить номер.
4. Вся цепочка — кто кому что поручил и в каком статусе — видна в общем
   своде в разделе «Кому как делегируются задачи» (КАУ/админы/наблюдатели).

Роли: `ADMIN` (полный доступ + видит делегирование), `KAU` (маршрутизация),
`GD` / `GR` / `OP` / `BTL_BF` (рабочие подразделения, видят только свой
кабинет), `VIEWER` (только просмотр общего свода и отчётов).

## Деплой на Railway (публичная ссылка)

Railway запускает приложение как обычный Node-процесс с диском — наш SQLite
работает без переделки на Postgres. Шаги:

1. На [railway.app](https://railway.app) → **New Project** → **Deploy from GitHub repo**
   → выбрать `sonkuzit00-arch/vnuuspam09`, ветку `claude/eloquent-noether-05ycbx`
   (или `main`, после мёрджа). Railway сам распознает Next.js через Nixpacks
   и подхватит `railway.toml` (команда запуска: `npm run deploy:start` —
   применяет миграции, сидит демо-данные и стартует сервер).
2. **Обязательно добавить Volume**, иначе база будет стираться при каждом
   передеплое: в настройках сервиса → **Volumes** → **New Volume**,
   mount path `/data`.
3. Задать переменные окружения (Settings → Variables):
   - `DATABASE_URL` = `file:/data/dev.db` (путь внутри примонтированного volume)
   - `NEXTAUTH_SECRET` = случайная строка (сгенерировать: `openssl rand -base64 32`)
   - `NEXTAUTH_URL` = публичный домен, который выдаст Railway после первого
     деплоя, например `https://vnuuspam09-production.up.railway.app`
     (можно сначала задеплоить, скопировать домен из Settings → Networking,
     затем вписать сюда и передеплоить ещё раз)
4. Deploy. После первого успешного запуска откроется публичная ссылка вида
   `https://<project>.up.railway.app/login` с демо-доступом из раздела выше.

## Структура

- `src/app/(app)/page.tsx` — личный кабинет: обращения + поручения; для КАУ
  дополнительно раздел «Требуют маршрутизации»
- `src/app/(app)/appeals/new` — регистрация обращения (источник сгруппирован
  по подразделениям; для ГД — 4 канала: СЭД «Дело», почта, эл. почта,
  соцсети — с делением на личные сообщения и комментарии)
- `src/app/(app)/appeals/[id]` — карточка обращения: маршрут КАУ, концепция/
  алгоритм, поручения (цепочка делегирования), передача ответственного,
  история взаимодействия, благодарность/СМИ
- `src/app/(app)/summary` — общий свод + «Кому как делегируются задачи»
  (КАУ/админ/наблюдатель)
- `src/app/(app)/reports` — отчёты за период + экспорт в CSV (КАУ/админ/наблюдатель)
- `prisma/schema.prisma` — модель данных: `Appeal`, `AppealResponsible`
  (история передачи ответственного), `AppealTask` (поручения), `AppealEvent`
  (CRM-история)
- `prisma/seed.ts` — 9 реальных пользователей команды + демо-цепочка
  делегирования (КАУ → GR → ГД)
